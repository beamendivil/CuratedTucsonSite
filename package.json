{
  "name": "arizona-wine-experience",
  "version": "1.0.0",
  "description": "Static website for Arizona wine experiences",
  "scripts": {
    "build": "npm run build:css",
    "build:css": "tailwindcss -i ./src/tailwind.css -o ./dist/tailwind.css --minify",
    "dev": "npm run build:css && python3 -m http.server 4173",
    "test": "node --test",
    "test:e2e": "playwright test",
    "check": "npm run build:css && npm run lint && npm run test && npm run test:e2e && npm run format:check",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write \"**/*.{html,js,css,md}\"",
    "format:check": "prettier --check \"**/*.{html,js,css,md}\""
  },
  "keywords": [
    "wine",
    "arizona",
    "static-site"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "autoprefixer": "^10.5.0",
    "eslint": "^8.56.0",
    "eslint-config-prettier": "^9.1.0",
    "postcss": "^8.5.14",
    "prettier": "^3.1.1",
    "tailwindcss": "^3.4.19"
  }
}
